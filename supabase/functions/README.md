# Supabase Edge Functions

Future Edge Functions for the games workspace belong here.

Default function layout:

```txt
supabase/functions/function-name/index.ts
```

Keep secrets in Supabase secrets or local env files, not in GitHub.
