# Supabase migrations

Migration SQL files created by the Supabase CLI belong in this directory.

Use this from the repository root when adding a new migration locally:

```bash
npx supabase migration new descriptive_name
```

Do not put secrets in migration files.
