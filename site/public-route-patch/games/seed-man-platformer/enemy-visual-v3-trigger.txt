Seed Man enemy visual v3 production trigger. Retry after fixing the release preparer so v3 now rebuilds the complete enemy renderer and preserves the v2 compatibility marker required by production validation.
