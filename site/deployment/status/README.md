# Deployment status artifacts

Machine-readable deployment status and verifier coverage files live in this directory. `public-suite-current-release-verifier.json` records the public game routes that receive strict current-release fingerprint checks after Public Suite V2 deployment.
