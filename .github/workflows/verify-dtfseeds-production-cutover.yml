name: Verify DTFSeeds Production Cutover

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/verify-dtfseeds-production-cutover.yml

permissions:
  contents: read

concurrency:
  group: verify-dtfseeds-production-cutover
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      SITE: https://dtfseeds.com
    steps:
      - name: Verify public routes from a fresh runner
        id: live
        shell: bash
        run: |
          set -uo pipefail
          out="$RUNNER_TEMP/dtf-live-verification.tsv"
          : > "$out"
          failures=0

          check_route() {
            local path="$1"
            local current="$2"
            local body="$RUNNER_TEMP/body-$(printf '%s' "$path" | tr '/?' '__').html"
            local headers="$RUNNER_TEMP/headers-$(printf '%s' "$path" | tr '/?' '__').txt"
            local status='000'
            local final_url=''
            local current_seen='no'
            local stale_seen='none'
            local ok='no'

            for attempt in 1 2 3 4 5 6; do
              token="${GITHUB_RUN_ID}-${attempt}-$(date +%s%N)"
              status="$(curl -4 -sS -L \
                --retry 2 --retry-delay 2 --connect-timeout 15 --max-time 45 \
                -D "$headers" -o "$body" -w '%{http_code}' \
                -H 'Cache-Control: no-cache, no-store, max-age=0' \
                -H 'Pragma: no-cache' \
                -H 'User-Agent: DTFSeeds-Live-Verification/3.0' \
                "$SITE${path}?dtf_verify=${token}" || printf '000')"
              final_url="$(awk 'BEGIN{IGNORECASE=1} /^location:/ {gsub(/\r/,""); loc=$2} END{print loc}' "$headers")"
              current_seen='no'
              if grep -Fqi -- "$current" "$body" 2>/dev/null; then current_seen='yes'; fi

              stale_seen='none'
              for stale in \
                'THC Grow Doc, genetics, cultivation education, and games in one home.' \
                'Grow education belongs in a clean, readable library.' \
                'MOPS, cultivation notes, THC basics' \
                'being rebuilt' \
                'Reserved strain card' \
                'Tool-ready rebuild'; do
                if grep -Fqi -- "$stale" "$body" 2>/dev/null; then
                  stale_seen="$stale"
                  break
                fi
              done

              if [[ "$status" =~ ^2[0-9][0-9]$ ]] && [[ "$current_seen" == 'yes' ]] && [[ "$stale_seen" == 'none' ]]; then
                ok='yes'
                break
              fi
              sleep 5
            done

            printf '%s\t%s\t%s\t%s\t%s\t%s\n' "$path" "$status" "$current_seen" "$stale_seen" "$ok" "$final_url" >> "$out"
            if [[ "$ok" != 'yes' ]]; then
              failures=$((failures+1))
              echo "::group::Live route evidence: $path" >&2
              printf 'status=%s expected=%q body_bytes=%s\n' "$status" "$current" "$(wc -c < "$body" 2>/dev/null || printf '0')" >&2
              echo 'Document landmarks:' >&2
              grep -Eio '<title[^>]*>[^<]{0,240}</title>|<h1[^>]*>[^<]{0,240}</h1>|data-[a-z0-9_-]+="[^"]{1,160}"' "$body" 2>/dev/null | head -n 40 >&2 || true
              echo 'Visible-text signature:' >&2
              sed -E 's/<script[^>]*>.*<\/script>//Ig; s/<style[^>]*>.*<\/style>//Ig; s/<[^>]+>/ /g; s/&nbsp;/ /g; s/&amp;/\&/g; s/[[:space:]]+/ /g' "$body" 2>/dev/null | fold -w 220 | head -n 8 >&2 || true
              echo '::endgroup::' >&2
            fi
          }

          check_route '/' 'Genetics first. Learn the plant behind the pack.'
          check_route '/learn/' 'Learn in a sequence that makes the plant easier to understand.'
          check_route '/learn/infographics/' 'Study question:'

          echo "report=$out" >> "$GITHUB_OUTPUT"
          echo "failures=$failures" >> "$GITHUB_OUTPUT"
          cat "$out"
          [[ "$failures" -eq 0 ]]

      - name: Report definitive live verification
        if: always()
        shell: bash
        env:
          OUTCOME: ${{ steps.live.outcome }}
          REPORT: ${{ steps.live.outputs.report }}
          FAILURES: ${{ steps.live.outputs.failures }}
        run: |
          {
            echo '## Definitive DTFSeeds live cutover verification'
            echo
            echo "- Run: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
            echo "- Overall: **${OUTCOME}**"
            echo "- Failed routes: **${FAILURES:-?}**"
            echo
            echo '| Route | HTTP | Current marker | Stale marker | Result | Redirect target |'
            echo '|---|---:|---|---|---|---|'
            if [[ -n "${REPORT:-}" && -f "$REPORT" ]]; then
              while IFS=$'\t' read -r path status current stale ok final_url; do
                [[ -n "$path" ]] || continue
                result='FAIL'; [[ "$ok" == 'yes' ]] && result='PASS'
                echo "| \`$path\` | ${status:-?} | ${current:-?} | ${stale:-none} | **$result** | ${final_url:--} |"
              done < "$REPORT"
            else
              echo '| no result | ? | ? | ? | **FAIL** | - |'
            fi
            echo
            echo 'Read-only visitor-facing verification using cache-busting URLs. Failed routes emit bounded document landmarks and visible-text evidence in the Actions log so ownership checks can be repaired without guessing or weakening the gate.'
          } >> "$GITHUB_STEP_SUMMARY"
