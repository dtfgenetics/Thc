Deploy the current validated Public Suite to production now after correcting stale Code Snippets bridge handling. Production safety is now verified by executability: every old exact-prefix DTF deployment bridge must be absent or inactive before publication; physical deletion is not required because this WordPress install returns 500 for DELETE on those stale rows. Preserve authenticated WordPress Application Password plus per-run token authorization, immutable app-only archive validation, transaction locking, rollback/recovery, and WordPress ownership of /, /learn/, /shop/, /seeds/, and /blog/. Publish the current unified DTF Genetics shell and application builds for /tools/, /games/, and /projects/, including High IQ, Protect the Plants/THC Grow Doc API, GrowLens, and all validated public game routes. Require independent feature-surface and canonical WordPress verification before reporting success. Retry after inactive-only safety gate correction.
