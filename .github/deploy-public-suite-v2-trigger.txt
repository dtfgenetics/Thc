Deploy the current validated Public Suite to production after adding transactional stale-lock recovery to the preflight cleanup. Old exact-prefix DTF bridge snippets must be non-executable; the cleanup may temporarily activate one trusted recent V2 bridge only to inspect the protected transaction lock, safely finalize an already-deployed/rolled-back stale transaction or invoke its persisted abort/rollback path for an interrupted transaction, verify the lock is clear with a disposable probe transaction, then deactivate the bridge again. Preserve WordPress Application Password + per-run token authorization, immutable app-only archive validation, rollback/recovery, and WordPress ownership of /, /learn/, /shop/, /seeds/, and /blog/. Publish and independently verify the current /tools/, /games/, /projects/, GrowLens, THC Grow Doc/Protect the Plants, High IQ, and other validated app routes before reporting success.
