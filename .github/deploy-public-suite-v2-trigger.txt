Deploy the current validated Public Suite to production now after fixing stale-bridge cleanup to resolve the installed Code Snippets plugin state before inspecting its REST namespace. Activate the already-installed plugin only when required for exact-prefix stale bridge cleanup, restore its original state, then let the transactional deployer manage its own temporary bridge lifecycle. Preserve authenticated WordPress Application Password plus per-run token authorization, immutable app-only archive validation, transaction locking, rollback/recovery, and WordPress ownership of /, /learn/, /shop/, /seeds/, and /blog/. Publish the current unified DTF Genetics shell and application builds for /tools/, /games/, and /projects/, including High IQ, Protect the Plants/THC Grow Doc API, GrowLens, and all validated public game routes. Require independent feature-surface and canonical WordPress verification before reporting success. Triggered 2026-08-24T08:08-05:00.
