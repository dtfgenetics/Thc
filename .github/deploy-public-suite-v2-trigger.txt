Deploy the current validated Public Suite using the fail-closed recent-ID recovery path when this installed Code Snippets version does not expose collection GET. The cleanup may create one inactive diagnostic marker, inspect only recent numeric snippet IDs, delete only exact stale names beginning with DTF Public Suite Deploy V2, remove its marker, and verify targeted stale IDs are gone before publication. Preserve authenticated WordPress Application Password plus per-run 256-bit token authorization, archive SHA/path allowlists, transaction locking and rollback/recovery, and WordPress ownership of /, /learn/, /shop/, /seeds/, and /blog/. Publish the unified shell for /tools/, /games/, and /projects/, including High IQ and Protect the Plants with its PHP API, then require independent feature and canonical-route verification. Triggered 2026-08-24T06:06-05:00.
