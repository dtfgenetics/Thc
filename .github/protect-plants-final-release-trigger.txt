Run the current-main Burn Buds production release through the rollback-capable WordPress Public Suite bridge while preserving the legacy /games/protect-the-plants/ compatibility route and multiplayer API. Require a fresh validated Public Suite build, all current multiplayer assets plus Burn Buds branding assets, transactional publication, canonical route protection, feature verification, and live multiplayer verification. Trigger revision: 10. Release: 2026-09-07. Reason: rerun the final production recovery after PR #779 corrected the production smoke’s deterministic opponent misses to stay inside the canonical 15x15 battle grid, matching the already-passing local multiplayer CI. The previous release successfully built, published, verified the full feature surface, and passed canonical WordPress ownership; its only failure was the verifier sending an invalid column -1 shot. This trigger starts the path-filtered Deploy Burn Buds Final Release workflow so the corrected production API and desktop/mobile UI acceptance can execute end to end.
