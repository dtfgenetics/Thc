import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
import process from 'node:process';

const validateOnly = process.argv.includes('--validate-only');
const site = (process.env.WP_SITE_URL || 'https://dtfseeds.com').replace(/\/$/, '');
const user = process.env.WP_API_USERNAME || '';
const pass = process.env.WP_API_PASSWORD || '';
const apply = String(process.env.APPLY_DIAGNOSTIC_CASE_LAB_V2 || '').toLowerCase() === 'true';
const basePath = process.env.DIAGNOSTIC_CASE_LAB_V1_PATH || 'site/wordpress/education/diagnostic-case-lab-v1.json';
const extensionPath = process.env.DIAGNOSTIC_CASE_LAB_V2_EXTENSION_PATH || 'site/wordpress/education/diagnostic-case-lab-v2-extension.json';
const backupRoot = process.env.BACKUP_ROOT || '/tmp/dtf-diagnostic-case-lab-v2';
const base = JSON.parse(await readFile(basePath, 'utf8'));
const extension = JSON.parse(await readFile(extensionPath, 'utf8'));
const data = {
  ...base,
  schemaVersion: 2,
  id: 'diagnostic-case-lab-v2',
  title: 'Diagnostic Case Lab',
  subtitle: 'Practice differential diagnosis with increasingly complex cases.',
  sourceRefs: [...base.sourceRefs, ...extension.sourceRefs],
  cases: [...base.cases, ...extension.cases],
  decisionTrees: [...base.decisionTrees, ...extension.decisionTrees]
};

const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
const esc = (v = '') => String(v).replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#039;');
const rendered = (v) => typeof v === 'string' ? v : (v?.raw || v?.rendered || '');

function validate(basePkg, extPkg, merged) {
  if (basePkg?.schemaVersion !== 1 || basePkg?.id !== 'diagnostic-case-lab-v1' || basePkg?.route !== '/learn/diagnostic-cases/' || basePkg?.slug !== 'diagnostic-cases') throw new Error('Invalid Diagnostic Case Lab V1 base identity.');
  if (extPkg?.schemaVersion !== 1 || extPkg?.id !== 'diagnostic-case-lab-v2-extension' || extPkg?.baseId !== 'diagnostic-case-lab-v1' || extPkg?.route !== '/learn/diagnostic-cases/') throw new Error('Invalid Diagnostic Case Lab V2 extension identity.');
  if (!Array.isArray(extPkg.sourceRefs) || extPkg.sourceRefs.length !== 6) throw new Error(`Expected exactly 6 V2 source references; found ${extPkg?.sourceRefs?.length || 0}.`);
  if (!Array.isArray(extPkg.cases) || extPkg.cases.length !== 12) throw new Error(`Expected exactly 12 V2 cases; found ${extPkg?.cases?.length || 0}.`);
  if (!Array.isArray(extPkg.decisionTrees) || extPkg.decisionTrees.length !== 6) throw new Error(`Expected exactly 6 V2 trees; found ${extPkg?.decisionTrees?.length || 0}.`);
  if (merged.sourceRefs.length !== 13) throw new Error(`Expected 13 merged sources; found ${merged.sourceRefs.length}.`);
  if (merged.cases.length !== 24) throw new Error(`Expected 24 merged cases; found ${merged.cases.length}.`);
  if (merged.decisionTrees.length !== 12) throw new Error(`Expected 12 merged trees; found ${merged.decisionTrees.length}.`);
  const sourceIds = new Set();
  for (const source of merged.sourceRefs) {
    if (!source.id || sourceIds.has(source.id) || !source.citation || !source.supports) throw new Error(`Invalid or duplicate source ${source.id}.`);
    sourceIds.add(source.id);
  }
  const caseIds = new Set(); let evidencePoints = 0; let differentials = 0;
  for (const c of merged.cases) {
    if (!c.id || caseIds.has(c.id)) throw new Error(`Duplicate/missing case id ${c.id}.`); caseIds.add(c.id);
    for (const field of ['title','difficulty','category','presenting','interpretation','why','action','verify']) if (!c[field]) throw new Error(`${c.id}: missing ${field}.`);
    if (!Array.isArray(c.evidence) || c.evidence.length < 4) throw new Error(`${c.id}: needs at least four evidence points.`);
    if (!Array.isArray(c.differential) || c.differential.length < 3) throw new Error(`${c.id}: needs at least three differential causes.`);
    if (!Array.isArray(c.sourceIds) || !c.sourceIds.length) throw new Error(`${c.id}: sourceIds required.`);
    for (const sid of c.sourceIds) if (!sourceIds.has(sid)) throw new Error(`${c.id}: unknown source ${sid}.`);
    for (const d of c.differential) if (!d.cause || !d.weight || !d.reason) throw new Error(`${c.id}: incomplete differential.`);
    evidencePoints += c.evidence.length; differentials += c.differential.length;
  }
  const treeIds = new Set(); let decisionNodes = 0;
  for (const tree of merged.decisionTrees) {
    if (!tree.id || treeIds.has(tree.id)) throw new Error(`Duplicate/missing tree id ${tree.id}.`); treeIds.add(tree.id);
    if (!tree.title || !tree.start || !Array.isArray(tree.nodes) || tree.nodes.length !== 5) throw new Error(`${tree.id}: expected title, start and exactly five nodes.`);
    const nodeIds = new Set();
    for (const node of tree.nodes) {
      if (!node.id || nodeIds.has(node.id) || !node.question || !node.yes || !node.no) throw new Error(`${tree.id}: invalid decision node.`);
      nodeIds.add(node.id); decisionNodes += 1;
    }
    if (!nodeIds.has(tree.start)) throw new Error(`${tree.id}: start node does not exist.`);
  }
  if (decisionNodes !== 60) throw new Error(`Expected 60 merged decision nodes; found ${decisionNodes}.`);
  const text = JSON.stringify(merged);
  for (const re of [/guaranteed\s+yield/i,/always\s+spray/i,/amber\s*=\s*couchlock/i,/one\s+photo\s+proves/i,/guaranteed\s+zero[-\s]risk\s+pollen/i,/fixed\s+safe\s+pollen\s+distance/i]) if (re.test(text)) throw new Error(`Unsupported positive claim found: ${re}`);
  return { cases: merged.cases.length, trees: merged.decisionTrees.length, decisionNodes, evidencePoints, differentials, sources: merged.sourceRefs.length, addedCases: extPkg.cases.length, addedTrees: extPkg.decisionTrees.length, addedSources: extPkg.sourceRefs.length };
}

const totals = validate(base, extension, data);
if (validateOnly) { console.log(JSON.stringify({ valid: true, id: data.id, route: data.route, ...totals }, null, 2)); process.exit(0); }
if (!apply) throw new Error('Set APPLY_DIAGNOSTIC_CASE_LAB_V2=true for production publication.');
if (!user || !pass) throw new Error('WP_API_USERNAME and WP_API_PASSWORD are required.');
const auth = `Basic ${Buffer.from(`${user}:${pass}`).toString('base64')}`;
async function request(path, options = {}) {
  let last;
  for (let attempt = 1; attempt <= 8; attempt++) {
    try {
      const response = await fetch(`${site}${path}`, { ...options, redirect:'follow', signal:AbortSignal.timeout(60000), headers:{ Authorization:auth, Accept:'application/json', 'User-Agent':'DTFSeeds-Diagnostic-Case-Lab-V2/1.0', ...(options.body?{'Content-Type':'application/json'}:{}), ...(options.headers||{}) } });
      const text = await response.text(); let body = text; try { body = text ? JSON.parse(text) : null; } catch {}
      if ((response.status === 429 || response.status >= 500) && attempt < 8) { await sleep(attempt * 1500); continue; }
      if (!response.ok) throw new Error(`${options.method || 'GET'} ${path} failed (${response.status}): ${typeof body === 'string' ? body.slice(0,700) : JSON.stringify(body).slice(0,700)}`);
      return body;
    } catch (error) { last = error; if (attempt < 8) await sleep(attempt * 1500); }
  }
  throw last;
}
async function rowsBySlug(slug) { const rows = await request(`/wp-json/wp/v2/pages?slug=${encodeURIComponent(slug)}&context=edit&per_page=100`); return Array.isArray(rows) ? rows : []; }
function badgeClass(weight='') { const w=weight.toLowerCase(); if (w.includes('higher')) return 'high'; if (w.includes('lower')) return 'low'; return 'mid'; }
function renderCase(c) {
  const wave = Number(c.id.replace(/\D/g,'')) >= 13 ? '<span class="dcl-new">New V2</span>' : '';
  return `<article class="dcl-case" data-dcl-case="${esc(c.id)}"><div class="dcl-case-head"><div><span class="dcl-id">${esc(c.id)}</span><span class="dcl-difficulty">${esc(c.difficulty)}</span>${wave}</div><span class="dcl-category">${esc(c.category)}</span></div><h3>${esc(c.title)}</h3><p class="dcl-presenting">${esc(c.presenting)}</p><div class="dcl-evidence"><strong>Evidence available</strong><ul>${c.evidence.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><details class="dcl-reveal"><summary>Reveal the differential and best-supported interpretation</summary><div class="dcl-differentials">${c.differential.map(d=>`<div class="dcl-diff"><span class="dcl-weight ${badgeClass(d.weight)}">${esc(d.weight)}</span><h4>${esc(d.cause)}</h4><p>${esc(d.reason)}</p></div>`).join('')}</div><div class="dcl-answer"><h4>Best-supported interpretation</h4><p>${esc(c.interpretation)}</p><p><strong>Why:</strong> ${esc(c.why)}</p></div><div class="dcl-next"><div><h4>Next justified action</h4><p>${esc(c.action)}</p></div><div><h4>Verify afterward</h4><p>${esc(c.verify)}</p></div></div><div class="dcl-links">${(c.relatedRoutes||[]).map(r=>`<a href="${esc(r)}">Open related subject →</a>`).join('')}</div></details></article>`;
}
function renderTree(tree) {
  const wave = Number(tree.id.replace(/\D/g,'')) >= 7 ? '<span class="dcl-new">New V2</span>' : '';
  return `<article class="dcl-tree" data-dcl-tree="${esc(tree.id)}"><div class="dcl-tree-title"><div><span>${esc(tree.id)}</span>${wave}</div><h3>${esc(tree.title)}</h3></div><div class="dcl-nodes">${tree.nodes.map((n,i)=>`<div class="dcl-node"><b>${i+1}</b><div><h4>${esc(n.question)}</h4><p><strong>Yes:</strong> ${esc(n.yes)}</p><p><strong>No:</strong> ${esc(n.no)}</p></div></div>${i<tree.nodes.length-1?'<div class="dcl-arrow">↓</div>':''}`).join('')}</div></article>`;
}
function sourceLink(s){ if(s.pmcid) return `https://pmc.ncbi.nlm.nih.gov/articles/${esc(s.pmcid)}/`; if(s.doi) return `https://doi.org/${esc(s.doi)}`; return ''; }
function renderPage(){
  const css=`<style id="dtf-diagnostic-case-lab-v2-style">.dcl{--ink:#102d1a;--muted:#53675a;--deep:#071b10;--green:#176d39;--gold:#d7b95f;--cream:#f8f5eb;--soft:#edf3ec;--line:#d4e1d6;background:var(--cream);color:var(--ink)}.dcl *{box-sizing:border-box}.dcl .wrap{width:min(1180px,calc(100% - 34px));margin:auto}.dcl .hero{padding:72px 0 60px;background:radial-gradient(circle at 82% 10%,rgba(215,185,95,.22),transparent 31%),linear-gradient(145deg,var(--deep),#103b23);color:#fff}.dcl .kicker{margin:0 0 10px;color:var(--gold);font-size:.76rem;font-weight:900;letter-spacing:.13em;text-transform:uppercase}.dcl h1{margin:0;font-size:clamp(2.8rem,6vw,5.3rem);line-height:.95;letter-spacing:-.055em}.dcl .lede{max-width:880px;color:#d5e4da;font-size:1.08rem;line-height:1.72}.dcl .stats{display:flex;gap:10px;flex-wrap:wrap;margin-top:25px}.dcl .stat{padding:10px 14px;border:1px solid rgba(255,255,255,.22);border-radius:999px;font-weight:900}.dcl .actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:23px}.dcl .btn{display:inline-flex;padding:10px 16px;border-radius:999px;text-decoration:none!important;font-weight:900}.dcl .primary{background:var(--gold);color:var(--deep)!important}.dcl .secondary{border:1px solid rgba(255,255,255,.28);color:#fff!important}.dcl .section{padding:64px 0}.dcl .soft{background:var(--soft)}.dcl .heading{display:flex;justify-content:space-between;align-items:end;gap:28px;margin-bottom:26px}.dcl .heading h2{margin:0;font-size:clamp(2rem,4vw,3.3rem);line-height:1.02;letter-spacing:-.04em}.dcl .heading p{max-width:600px;margin:0;color:var(--muted);line-height:1.65}.dcl .v2-banner{margin-top:26px;padding:20px;border:1px solid rgba(215,185,95,.38);border-radius:18px;background:rgba(215,185,95,.09)}.dcl .v2-banner strong{color:var(--gold)}.dcl .rules{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px}.dcl .rule,.dcl-case,.dcl-tree,.dcl-source{background:#fff;border:1px solid var(--line);border-radius:18px}.dcl .rule{padding:20px}.dcl .rule b{display:grid;place-items:center;width:36px;height:36px;border-radius:11px;background:#e8efe8;color:var(--green);margin-bottom:10px}.dcl .rule h3{margin:0 0 7px}.dcl .rule p{margin:0;color:var(--muted);line-height:1.58}.dcl .case-grid,.dcl .tree-grid,.dcl .sources{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.dcl-case,.dcl-tree{padding:22px}.dcl-case{box-shadow:0 10px 28px rgba(16,48,28,.055)}.dcl-case-head{display:flex;justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap}.dcl-id,.dcl-difficulty,.dcl-category,.dcl-new{display:inline-block;padding:5px 9px;border-radius:999px;font-size:.72rem;font-weight:900}.dcl-id{background:#102d1a;color:#fff}.dcl-difficulty{background:#edf3ec;margin-left:6px}.dcl-new{margin-left:6px;background:#d7b95f;color:#071b10!important}.dcl-category{background:#f6efd8;color:#6a5615}.dcl-case h3{font-size:1.38rem;margin:14px 0 8px}.dcl-presenting{color:var(--muted);line-height:1.65}.dcl-evidence{background:#f3f6f1;border-radius:15px;padding:15px;margin:15px 0}.dcl-evidence ul{margin:8px 0 0;padding-left:18px;color:var(--muted);line-height:1.55}.dcl-reveal{border-top:1px solid var(--line);padding-top:13px}.dcl-reveal summary{cursor:pointer;color:var(--green);font-weight:900}.dcl-differentials{display:grid;gap:10px;margin-top:16px}.dcl-diff{position:relative;border:1px solid var(--line);border-radius:14px;padding:14px}.dcl-diff h4{margin:0 0 5px;padding-right:85px}.dcl-diff p{margin:0;color:var(--muted);line-height:1.55}.dcl-weight{position:absolute;right:12px;top:12px;padding:4px 7px;border-radius:999px;font-size:.68rem;font-weight:900}.dcl-weight.high{background:#dff0df;color:#176d39}.dcl-weight.mid{background:#f6efd8;color:#6a5615}.dcl-weight.low{background:#f2e6e3;color:#7a382e}.dcl-answer{margin-top:14px;padding:16px;border-radius:15px;background:#0d2a19;color:#fff}.dcl-answer h4{margin:0 0 7px}.dcl-answer p{margin:6px 0;color:#d5e1d8;line-height:1.58}.dcl-next{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:10px}.dcl-next>div{padding:14px;border-radius:14px;background:#f3f6f1}.dcl-next h4{margin:0 0 6px}.dcl-next p{margin:0;color:var(--muted);line-height:1.55}.dcl-links{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}.dcl-links a,.dcl-source a{font-weight:900;color:var(--green)!important;text-decoration:none!important}.dcl-tree-title span{font-size:.72rem;font-weight:900;color:var(--green)}.dcl-tree-title h3{margin:5px 0 16px;font-size:1.4rem}.dcl-node{display:grid;grid-template-columns:38px 1fr;gap:11px;padding:13px;border-radius:15px;background:#f3f6f1}.dcl-node>b{display:grid;place-items:center;width:34px;height:34px;border-radius:10px;background:var(--green);color:#fff}.dcl-node h4{margin:0 0 6px}.dcl-node p{margin:4px 0;color:var(--muted);line-height:1.5}.dcl-arrow{text-align:center;color:var(--green);font-size:1.3rem;font-weight:900;padding:2px}.dcl-source{padding:18px}.dcl-source h3{margin:0 0 6px;font-size:1.02rem}.dcl-source p{margin:6px 0;color:var(--muted);line-height:1.55}.dcl .boundary{padding:28px;border-radius:24px;background:#0d2a19;color:#fff}.dcl .boundary h2{margin:0 0 8px}.dcl .boundary p{color:#d4e2d8;line-height:1.65;max-width:900px}@media(max-width:980px){.dcl .case-grid,.dcl .tree-grid,.dcl .sources{grid-template-columns:1fr}.dcl .rules{grid-template-columns:repeat(2,minmax(0,1fr))}.dcl .heading{align-items:flex-start;flex-direction:column}}@media(max-width:650px){.dcl .wrap{width:min(100% - 26px,1180px)}.dcl .hero{padding:52px 0 44px}.dcl .section{padding:48px 0}.dcl .rules,.dcl-next{grid-template-columns:1fr}.dcl .actions .btn{width:100%;justify-content:center}}</style>`;
  const rules=[['Describe first','Name the organ, color, shape, distribution and progression before assigning a cause.'],['Compare causes','Keep plausible alternatives alive until evidence separates them.'],['Measure next','Choose measurements that can actually change the ranking of the differential.'],['Verify change','Judge success from new growth, incidence, counts or measurements—not hope.']];
  const sources=data.sourceRefs.map(s=>{const url=sourceLink(s);return `<article class="dcl-source"><h3>${esc(s.id)} · ${esc(s.citation)}</h3><p>${esc(s.supports)}</p>${url?`<a href="${url}" target="_blank" rel="noopener noreferrer">Open source →</a>`:''}</article>`}).join('');
  return `${css}<main class="dcl" data-dtf-diagnostic-case-lab-v2="true"><section class="hero"><div class="wrap"><p class="kicker">Teaching Healthy Cultivation · Diagnostic Case Lab V2</p><h1>${esc(data.title)}</h1><p class="lede">${esc(data.learningOutcome)}</p><div class="stats"><span class="stat">24 worked cases</span><span class="stat">12 decision trees</span><span class="stat">60 decision nodes</span><span class="stat">13 evidence sources</span></div><div class="v2-banner"><strong>V2 expansion:</strong> propagation, germination, transplant establishment, pH/EC sampling, root-zone concentration, aphids, unexpected pollination, outdoor weather stress, and postharvest drying/curing are now included.</div><div class="actions"><a class="btn primary" href="#cases">Start a case</a><a class="btn secondary" href="/learn/diagnostics/">Open Diagnostic Index</a><a class="btn secondary" href="/thc-grow-doc/">Open Grow Doc</a></div></div></section><section class="section"><div class="wrap"><div class="heading"><div><p class="kicker">Diagnostic method</p><h2>Reason before you treat.</h2></div><p>${esc(data.evidenceBoundary)}</p></div><div class="rules">${rules.map((r,i)=>`<article class="rule"><b>${i+1}</b><h3>${esc(r[0])}</h3><p>${esc(r[1])}</p></article>`).join('')}</div></div></section><section class="section soft" id="cases"><div class="wrap"><div class="heading"><div><p class="kicker">Worked cases</p><h2>Try the diagnosis before revealing the answer.</h2></div><p>Cases 13–24 extend the lab into propagation, reproductive, outdoor and postharvest problems while preserving the original case set.</p></div><div class="case-grid">${data.cases.map(renderCase).join('')}</div></div></section><section class="section"><div class="wrap"><div class="heading"><div><p class="kicker">Decision trees</p><h2>Choose the next observation that reduces uncertainty.</h2></div><p>The trees guide investigation; they do not convert one symptom or instrument reading into an automatic diagnosis.</p></div><div class="tree-grid">${data.decisionTrees.map(renderTree).join('')}</div></div></section><section class="section soft"><div class="wrap"><div class="boundary"><p class="kicker">Evidence boundary</p><h2>A symptom can narrow the question without proving the answer.</h2><p>${esc(data.evidenceBoundary)}</p><div class="actions"><a class="btn primary" href="/learn/research-methods/">Study Evidence & Measurement</a><a class="btn secondary" href="/learn/records/">Open Grow Records</a></div></div></div></section><section class="section"><div class="wrap"><div class="heading"><div><p class="kicker">Research base</p><h2>Sources behind the case logic.</h2></div><p>The V2 source set includes peer-reviewed cannabis/hemp work on pests and disease, nutrition, lighting, harvest, germination, cloning, sex expression, pollen dispersal, outdoor weather stress and postharvest processing.</p></div><div class="sources">${sources}</div></div></section></main>`;
}
function integrationBlock(){return `<!-- DTF-DIAGNOSTIC-CASE-LAB-LINK-V2:START --><style id="dtf-diagnostic-case-lab-link-v2-style">.dx5 .dcl-entry{margin-top:24px;padding:24px;border-radius:22px;background:linear-gradient(135deg,#102d1a,#176d39);color:#fff}.dx5 .dcl-entry h3{margin:0 0 8px;font-size:1.55rem}.dx5 .dcl-entry p{margin:0;color:#d9e6dd;line-height:1.6;max-width:820px}.dx5 .dcl-entry a{display:inline-block;margin-top:13px;padding:10px 15px;border-radius:999px;background:#d7b95f;color:#071b10!important;text-decoration:none!important;font-weight:900}</style><section class="dcl-entry" data-dtf-diagnostic-cases-link-v2="true"><h3>Practice with 24 worked diagnostic cases</h3><p>Use 24 evidence-backed cases and 12 decision trees to compare look-alike causes, choose the next useful measurement, and verify whether your interpretation survives follow-up evidence.</p><a href="/learn/diagnostic-cases/">Open Diagnostic Case Lab →</a></section><!-- DTF-DIAGNOSTIC-CASE-LAB-LINK-V2:END -->`;}
function addOrReplaceIntegration(raw){const block=integrationBlock();const existing=/<!-- DTF-DIAGNOSTIC-CASE-LAB-LINK-V(?:1|2):START -->[\s\S]*?<!-- DTF-DIAGNOSTIC-CASE-LAB-LINK-V(?:1|2):END -->/;if(existing.test(raw))return raw.replace(existing,block);const marker='</main>';if(raw.includes(marker))return raw.replace(marker,`${block}${marker}`);return `${raw}${block}`;}
const learnRows=await rowsBySlug('learn');if(learnRows.length!==1)throw new Error(`Expected exactly one Learn page; found ${learnRows.length}.`);const learn=learnRows[0];
const caseRows=await rowsBySlug('diagnostic-cases');if(caseRows.length!==1)throw new Error(`Expected exactly one existing diagnostic-cases page; found ${caseRows.length}.`);const casePage=caseRows[0];if(Number(casePage.parent)!==Number(learn.id))throw new Error('Diagnostic Case Lab is outside the Learn hierarchy; refusing write.');const oldCaseRaw=rendered(casePage.content);if(!oldCaseRaw.includes('data-dtf-diagnostic-case-lab-v1="true"')&&!oldCaseRaw.includes('data-dtf-diagnostic-case-lab-v2="true"'))throw new Error('Diagnostic Case Lab ownership marker missing; refusing replacement.');
const diagnosticRows=await rowsBySlug('diagnostics');if(diagnosticRows.length!==1)throw new Error(`Expected exactly one diagnostics page; found ${diagnosticRows.length}.`);const diagnosticPage=diagnosticRows[0];if(Number(diagnosticPage.parent)!==Number(learn.id))throw new Error('Diagnostics page is outside the Learn hierarchy; refusing write.');const oldDiagnosticRaw=rendered(diagnosticPage.content);if(!oldDiagnosticRaw.includes('data-dtf-diagnostic-index-v5="true"'))throw new Error('Diagnostic Index V5 ownership marker missing; refusing integration update.');
const stamp=new Date().toISOString().replace(/[-:.]/g,'');const backupDir=join(backupRoot,`diagnostic-case-lab-v2-${stamp}`);await mkdir(backupDir,{recursive:true});await writeFile(join(backupDir,'case-lab-before.html'),oldCaseRaw);await writeFile(join(backupDir,'diagnostic-index-before.html'),oldDiagnosticRaw);
const newCaseRaw=renderPage();const newDiagnosticRaw=addOrReplaceIntegration(oldDiagnosticRaw);await writeFile(join(backupDir,'case-lab-planned.html'),newCaseRaw);await writeFile(join(backupDir,'diagnostic-index-planned.html'),newDiagnosticRaw);
async function updatePage(id,title,content){return request(`/wp-json/wp/v2/pages/${id}`,{method:'POST',body:JSON.stringify({title,content,status:'publish'})});}
async function restore(){const errors=[];try{await updatePage(casePage.id,rendered(casePage.title)||'Diagnostic Case Lab',oldCaseRaw)}catch(error){errors.push(`Case Lab restore failed: ${error.message}`)}try{await updatePage(diagnosticPage.id,rendered(diagnosticPage.title)||'Diagnostics',oldDiagnosticRaw)}catch(error){errors.push(`Diagnostic Index restore failed: ${error.message}`)}if(errors.length)throw new Error(errors.join(' | '));}
try{await updatePage(casePage.id,'Diagnostic Case Lab',newCaseRaw);await updatePage(diagnosticPage.id,rendered(diagnosticPage.title)||'Diagnostics',newDiagnosticRaw);const checkCaseRows=await rowsBySlug('diagnostic-cases');const checkDiagnosticRows=await rowsBySlug('diagnostics');if(checkCaseRows.length!==1||checkDiagnosticRows.length!==1)throw new Error('Post-write page lookup became ambiguous.');const checkedCase=rendered(checkCaseRows[0].content);const checkedDiagnostic=rendered(checkDiagnosticRows[0].content);if(!checkedCase.includes('data-dtf-diagnostic-case-lab-v2="true"'))throw new Error('V2 Case Lab ownership marker missing after write.');if((checkedCase.match(/data-dcl-case=/g)||[]).length!==24)throw new Error('REST verification did not find exactly 24 cases.');if((checkedCase.match(/data-dcl-tree=/g)||[]).length!==12)throw new Error('REST verification did not find exactly 12 decision trees.');if((checkedCase.match(/class="dcl-node"/g)||[]).length!==60)throw new Error('REST verification did not find exactly 60 decision nodes.');if((checkedCase.match(/class="dcl-source"/g)||[]).length!==13)throw new Error('REST verification did not find exactly 13 evidence sources.');if(!checkedDiagnostic.includes('data-dtf-diagnostic-cases-link-v2="true"'))throw new Error('Diagnostic Index V2 Case Lab link missing after write.');if((checkedDiagnostic.match(/data-dtf-diagnostic-cases-link-v2=/g)||[]).length!==1)throw new Error('Diagnostic Index V2 link is duplicated.');const report={result:'success',route:'/learn/diagnostic-cases/',diagnosticIndexRoute:'/learn/diagnostics/',pageId:casePage.id,diagnosticPageId:diagnosticPage.id,...totals,backupDir,publishedAt:new Date().toISOString()};await writeFile(join(backupDir,'report.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));}catch(error){let rollbackError='';try{await restore()}catch(rollback){rollbackError=` Rollback error: ${rollback.message}`}await writeFile(join(backupDir,'failure.txt'),`${error.stack||error.message}${rollbackError}\n`);throw new Error(`${error.message}${rollbackError}`);}
