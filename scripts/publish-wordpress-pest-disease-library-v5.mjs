import { readFile, writeFile, mkdir } from 'node:fs/promises';
import { join } from 'node:path';
import process from 'node:process';

const siteUrl=(process.env.WP_SITE_URL||'https://dtfseeds.com').replace(/\/$/,'');
const username=process.env.WP_API_USERNAME||'';
const password=process.env.WP_API_PASSWORD||'';
const apply=String(process.env.APPLY_PEST_DISEASE_V5||'').toLowerCase()==='true';
const sourcePath=process.env.PEST_DISEASE_V5_PATH||'site/wordpress/education/pest-disease-profiles-v5.json';
const backupRoot=process.env.BACKUP_ROOT||'/tmp/dtf-pest-disease-v5';
if(!username||!password) throw new Error('WP_API_USERNAME and WP_API_PASSWORD are required.');
const data=JSON.parse(await readFile(sourcePath,'utf8'));
if(data.schemaVersion!==1||!Array.isArray(data.profiles)||data.profiles.length<10) throw new Error('Invalid pest/disease profile catalog.');
const auth=`Basic ${Buffer.from(`${username}:${password}`).toString('base64')}`;
const headers={Authorization:auth,Accept:'application/json','User-Agent':'DTFSeeds-Pest-Disease-V5/1.0'};
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const esc=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const stamp=new Date().toISOString().replace(/[-:.]/g,'');
const backupDir=join(backupRoot,`pest-disease-v5-${stamp}`);await mkdir(backupDir,{recursive:true});
async function request(path,options={}){let last;for(let a=1;a<=8;a++){try{const r=await fetch(`${siteUrl}${path}`,{...options,redirect:'follow',signal:AbortSignal.timeout(60000),headers:{...headers,...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})}});const t=await r.text();let b=t;try{b=t?JSON.parse(t):null}catch{}if((r.status===429||r.status>=500)&&a<8){await sleep(a*1600);continue}if(!r.ok)throw new Error(`${options.method||'GET'} ${path} failed (${r.status}): ${typeof b==='string'?b.slice(0,500):JSON.stringify(b).slice(0,500)}`);return b}catch(e){last=e;if(a<8)await sleep(a*1600)}}throw last}
async function bySlug(slug){const rows=await request(`/wp-json/wp/v2/pages?slug=${encodeURIComponent(slug)}&context=edit&per_page=50`);return Array.isArray(rows)?rows:[]}
async function ensurePage(slug,title,parent){const rows=(await bySlug(slug)).filter(p=>Number(p.parent)===Number(parent));if(rows.length>1)throw new Error(`${slug}: ambiguous duplicate pages`);if(rows[0])return rows[0];if(!apply)return{id:null,slug,parent,title:{rendered:title},content:{rendered:''}};return request('/wp-json/wp/v2/pages',{method:'POST',body:JSON.stringify({slug,title,parent,status:'publish',content:''})})}
function css(id){return `<style id="dtf-pd-v5-style">body.page-id-${id} .entry-title,body.page-id-${id} .wp-block-post-title{display:none!important}.pd5{--ink:#12311d;--deep:#071b10;--green:#176d39;--gold:#d7b95f;--cream:#f7f4ea;--soft:#edf3ec;--line:#d5e1d7;--muted:#526457;background:var(--cream);color:var(--ink)}.pd5 *{box-sizing:border-box}.pd5 .wrap{width:min(1160px,calc(100% - 34px));margin:auto}.pd5 .hero{padding:68px 0 58px;background:radial-gradient(circle at 84% 10%,rgba(215,185,95,.2),transparent 30%),linear-gradient(145deg,var(--deep),#103b23);color:#fff}.pd5 .kicker{margin:0 0 10px;color:var(--gold);font-size:.76rem;font-weight:900;letter-spacing:.13em;text-transform:uppercase}.pd5 h1{margin:0;font-size:clamp(2.7rem,5.8vw,5rem);line-height:.96;letter-spacing:-.05em}.pd5 .lede{max-width:820px;color:#d5e2d9;font-size:1.06rem;line-height:1.7}.pd5 .section{padding:62px 0}.pd5 .soft{background:var(--soft)}.pd5 .grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}.pd5 .card,.pd5 .panel{background:#fff;border:1px solid var(--line);border-radius:20px;padding:21px;box-shadow:0 10px 26px rgba(16,48,28,.05)}.pd5 .card h3,.pd5 .panel h2{margin:0 0 8px}.pd5 .card p,.pd5 .panel p,.pd5 .panel li{color:var(--muted);line-height:1.6}.pd5 .tag{display:inline-flex;padding:5px 9px;border-radius:999px;background:#e8efe8;color:var(--green);font-size:.69rem;font-weight:900;text-transform:uppercase;letter-spacing:.08em}.pd5 .card a,.pd5 .text-link{display:inline-block;margin-top:11px;color:var(--green)!important;text-decoration:none!important;font-weight:900}.pd5 .detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.pd5 .panel ul{margin:0;padding-left:19px}.pd5 .actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:23px}.pd5 .btn{display:inline-flex;padding:10px 16px;border-radius:999px;text-decoration:none!important;font-weight:900;border:1px solid rgba(255,255,255,.25);color:#fff!important}.pd5 .btn.primary{background:var(--gold);color:var(--deep)!important;border-color:var(--gold)}.pd5 .note{padding:24px;border-radius:22px;background:#0e2b1a;color:#fff}.pd5 .note p{color:#c6d6cb;line-height:1.65}@media(max-width:900px){.pd5 .grid{grid-template-columns:repeat(2,minmax(0,1fr))}.pd5 .detail-grid{grid-template-columns:1fr}}@media(max-width:620px){.pd5 .wrap{width:min(100% - 26px,1160px)}.pd5 .hero{padding:50px 0 44px}.pd5 .section{padding:48px 0}.pd5 .grid{grid-template-columns:1fr}.pd5 .actions .btn{width:100%;justify-content:center}}</style>`}
function hubHtml(id){return `${css(id)}<main class="pd5" data-dtf-pd-v5="hub"><section class="hero"><div class="wrap"><p class="kicker">Teaching Healthy Cultivation · Pest & Disease Library</p><h1>Identify the organism or disorder before choosing a response.</h1><p class="lede">These profiles are evidence-first references. A symptom photo can suggest possibilities, but confirmation may require magnification, repeated scouting, microscopy, molecular testing, or a diagnostic laboratory.</p><div class="actions"><a class="btn primary" href="/learn/diagnostics/">Open Diagnostic Index</a><a class="btn" href="/learn/ipm/">Plant Health & IPM</a><a class="btn" href="/learn/encyclopedia/ipm-biosecurity/">IPM Encyclopedia</a></div></div></section><section class="section soft"><div class="wrap"><div class="grid">${data.profiles.map(p=>`<article class="card"><span class="tag">${esc(p.category)}</span><h3>${esc(p.title)}</h3><p>${esc(p.summary)}</p><a href="/learn/pests-diseases/${esc(p.slug)}/">Open profile →</a></article>`).join('')}</div></div></section><section class="section"><div class="wrap"><div class="note"><h2>Use the same evidence rule for every profile.</h2><p>Record what you actually see, compare look-alikes, collect evidence that can separate them, and recheck after any intervention. Presence of an organism does not automatically prove it caused every symptom on the plant.</p></div></div></section></main>`}
function profileHtml(id,p){const block=(title,items)=>`<article class="panel"><h2>${esc(title)}</h2><ul>${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article>`;return `${css(id)}<main class="pd5" data-dtf-pd-v5="profile" data-profile="${esc(p.slug)}"><section class="hero"><div class="wrap"><p class="kicker">Pest & Disease Profile · ${esc(p.category)}</p><h1>${esc(p.title)}</h1><p class="lede">${esc(p.summary)}</p><div class="actions"><a class="btn primary" href="/learn/pests-diseases/">All profiles</a><a class="btn" href="/learn/diagnostics/">Diagnostic Index</a><a class="btn" href="/learn/ipm/">Plant Health & IPM</a></div></div></section><section class="section soft"><div class="wrap"><div class="detail-grid">${block('What to observe',p.observe)}${block('Common look-alikes',p.lookAlikes)}${block('Evidence that improves confirmation',p.confirm)}${block('Risk context',p.riskContext)}${block('Management principles',p.managementPrinciples)}<article class="panel"><h2>Keep the conclusion proportional to the evidence</h2><p>Use a confirmed identification when available. When evidence is incomplete, label the conclusion as a suspicion or differential rather than presenting it as proven.</p><a class="text-link" href="/learn/encyclopedia/pests/">Pest encyclopedia →</a><br><a class="text-link" href="/learn/encyclopedia/diseases-viroids/">Disease & viroid encyclopedia →</a></article></div></div></section></main>`}
const learn=(await bySlug('learn'))[0];if(!learn?.id)throw new Error('Learn parent missing');
const hub=await ensurePage('pests-diseases','Pest & Disease Identification — Teaching Healthy Cultivation',learn.id);if(!hub?.id)throw new Error('Could not resolve pest/disease hub');
const touched=[];for(const item of [hub])if(item?.id){await writeFile(join(backupDir,`page-${item.id}-${item.slug}-before.json`),`${JSON.stringify(item,null,2)}\n`)}
if(apply)await request(`/wp-json/wp/v2/pages/${hub.id}`,{method:'POST',body:JSON.stringify({title:'Pest & Disease Identification — Teaching Healthy Cultivation',content:hubHtml(hub.id),status:'publish'})});
const results=[];for(const p of data.profiles){const page=await ensurePage(p.slug,p.title,hub.id);if(!page?.id)throw new Error(`${p.slug}: page unresolved`);await writeFile(join(backupDir,`page-${page.id}-${p.slug}-before.json`),`${JSON.stringify(page,null,2)}\n`);if(apply)await request(`/wp-json/wp/v2/pages/${page.id}`,{method:'POST',body:JSON.stringify({title:p.title,content:profileHtml(page.id,p),status:'publish',parent:hub.id})});results.push({id:page.id,slug:p.slug,url:`${siteUrl}/learn/pests-diseases/${p.slug}/`})}
if(apply){const targets=[{url:`${siteUrl}/learn/pests-diseases/`,marker:'data-dtf-pd-v5="hub"'},...results.slice(0,4).map(x=>({url:x.url,marker:'data-dtf-pd-v5="profile"'}))];for(const t of targets){let ok=false;for(let a=1;a<=8;a++){try{const r=await fetch(`${t.url}?dtf_pd5=${Date.now()}-${a}`,{redirect:'follow',signal:AbortSignal.timeout(60000),headers:{'Cache-Control':'no-cache, no-store, max-age=0','Pragma':'no-cache'}});const h=await r.text();if(r.ok&&h.includes(t.marker)){ok=true;break}}catch{}await sleep(a*2200)}if(!ok)throw new Error(`Visitor verification failed: ${t.url}`)}}
const report={generatedAt:new Date().toISOString(),apply,hubId:hub.id,profileCount:results.length,results};await writeFile(join(backupDir,'pest-disease-v5-report.json'),`${JSON.stringify(report,null,2)}\n`);console.log(JSON.stringify(report,null,2));
