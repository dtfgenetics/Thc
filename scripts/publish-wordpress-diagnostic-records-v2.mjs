import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
import process from 'node:process';

const validateOnly=process.argv.includes('--validate-only');
const site=(process.env.WP_SITE_URL||'https://dtfseeds.com').replace(/\/$/,'');
const user=process.env.WP_API_USERNAME||'';
const pass=process.env.WP_API_PASSWORD||'';
const apply=String(process.env.APPLY_DIAGNOSTIC_RECORDS_V2||'').toLowerCase()==='true';
const sourcePath=process.env.DIAGNOSTIC_RECORDS_V2_PATH||'site/wordpress/education/diagnostic-records-v2.json';
const backupRoot=process.env.BACKUP_ROOT||'/tmp/dtf-diagnostic-records-v2';
const data=JSON.parse(await readFile(sourcePath,'utf8'));
const esc=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const rendered=(v)=>typeof v==='string'?v:(v?.raw||v?.rendered||'');
const sleep=(ms)=>new Promise(r=>setTimeout(r,ms));

function validate(pkg){
  if(pkg?.schemaVersion!==1||pkg?.id!=='diagnostic-records-v2'||pkg?.route!=='/learn/records/')throw new Error('Invalid Diagnostic Records V2 identity.');
  if(!pkg.title||!pkg.subtitle||!pkg.evidenceBoundary)throw new Error('Records package needs title, subtitle and evidence boundary.');
  if(!Array.isArray(pkg.templates)||pkg.templates.length!==10)throw new Error(`Expected 10 templates; found ${pkg?.templates?.length||0}.`);
  const ids=new Set();let fields=0;
  for(const t of pkg.templates){
    if(!t.id||ids.has(t.id)||!t.title||!t.purpose||!t.verificationPrompt)throw new Error(`Invalid template ${t.id}.`);ids.add(t.id);
    if(!Array.isArray(t.fields)||t.fields.length!==7)throw new Error(`${t.id}: expected exactly 7 fields.`);
    for(const f of t.fields){if(!f.label||!f.help)throw new Error(`${t.id}: incomplete field.`);fields++;}
  }
  if(fields!==70)throw new Error(`Expected exactly 70 fields; found ${fields}.`);
  if(!Array.isArray(pkg.supportRoutes)||pkg.supportRoutes.length!==8)throw new Error(`Expected exactly 8 support routes; found ${pkg?.supportRoutes?.length||0}.`);
  const text=JSON.stringify(pkg);
  for(const re of [/guaranteed\s+diagnosis/i,/one\s+photo\s+proves/i,/universal\s+lockout\s+pH/i,/always\s+spray/i])if(re.test(text))throw new Error(`Unsupported positive claim: ${re}`);
  return {templates:pkg.templates.length,fields,supportRoutes:pkg.supportRoutes.length};
}
const totals=validate(data);
if(validateOnly){console.log(JSON.stringify({valid:true,id:data.id,route:data.route,...totals},null,2));process.exit(0);}
if(!apply)throw new Error('Set APPLY_DIAGNOSTIC_RECORDS_V2=true for production publication.');
if(!user||!pass)throw new Error('WP_API_USERNAME and WP_API_PASSWORD are required.');
const auth=`Basic ${Buffer.from(`${user}:${pass}`).toString('base64')}`;
async function request(path,options={}){let last;for(let attempt=1;attempt<=8;attempt++){try{const res=await fetch(`${site}${path}`,{...options,redirect:'follow',signal:AbortSignal.timeout(60000),headers:{Authorization:auth,Accept:'application/json','User-Agent':'DTFSeeds-Diagnostic-Records-V2/1.0',...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})}});const txt=await res.text();let body=txt;try{body=txt?JSON.parse(txt):null}catch{}if((res.status===429||res.status>=500)&&attempt<8){await sleep(attempt*1500);continue}if(!res.ok)throw new Error(`${options.method||'GET'} ${path} failed (${res.status}): ${typeof body==='string'?body.slice(0,650):JSON.stringify(body).slice(0,650)}`);return body}catch(e){last=e;if(attempt<8)await sleep(attempt*1500)}}throw last}
async function rowsBySlug(slug){const rows=await request(`/wp-json/wp/v2/pages?slug=${encodeURIComponent(slug)}&context=edit&per_page=100`);return Array.isArray(rows)?rows:[]}
function renderTemplate(t,index){return `<article class="dr2-template" data-dr2-template="${esc(t.id)}"><header><div><span class="dr2-id">${esc(t.id)}</span><span class="dr2-num">Worksheet ${index+1} of 10</span></div><h3>${esc(t.title)}</h3><p>${esc(t.purpose)}</p></header><div class="dr2-fields">${t.fields.map(f=>`<div class="dr2-field"><strong>${esc(f.label)}</strong><small>${esc(f.help)}</small><div class="dr2-write" aria-hidden="true"></div></div>`).join('')}</div><div class="dr2-check"><strong>Verification question</strong><p>${esc(t.verificationPrompt)}</p><div class="dr2-choice">□ Yes &nbsp;&nbsp; □ No &nbsp;&nbsp; □ Unresolved / needs more evidence</div></div></article>`}
function renderPage(){const css=`<style id="dtf-diagnostic-records-v2-style">.dr2{--ink:#102d1a;--muted:#58675d;--deep:#071b10;--green:#176d39;--gold:#d7b95f;--cream:#f8f5eb;--soft:#edf3ec;--line:#cfdccf;background:var(--cream);color:var(--ink)}.dr2 *{box-sizing:border-box}.dr2 .wrap{width:min(1180px,calc(100% - 34px));margin:auto}.dr2 .hero{padding:70px 0 58px;background:radial-gradient(circle at 84% 10%,rgba(215,185,95,.2),transparent 32%),linear-gradient(145deg,var(--deep),#103b23);color:#fff}.dr2 .kicker{margin:0 0 10px;color:var(--gold);font-size:.76rem;font-weight:900;letter-spacing:.13em;text-transform:uppercase}.dr2 h1{margin:0;font-size:clamp(2.7rem,6vw,5.2rem);line-height:.96;letter-spacing:-.05em}.dr2 .lede{max-width:860px;color:#d4e2d8;font-size:1.08rem;line-height:1.7}.dr2 .stats,.dr2 .actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:22px}.dr2 .stat{padding:9px 13px;border:1px solid rgba(255,255,255,.24);border-radius:999px;font-weight:900}.dr2 .btn{display:inline-flex;padding:10px 16px;border-radius:999px;text-decoration:none!important;font-weight:900}.dr2 .primary{background:var(--gold);color:var(--deep)!important}.dr2 .secondary{border:1px solid rgba(255,255,255,.28);color:#fff!important}.dr2 .section{padding:60px 0}.dr2 .soft{background:var(--soft)}.dr2 .heading{display:flex;justify-content:space-between;align-items:end;gap:26px;margin-bottom:25px}.dr2 .heading h2{margin:0;font-size:clamp(2rem,4vw,3.2rem);letter-spacing:-.04em;line-height:1.02}.dr2 .heading p{max-width:580px;margin:0;color:var(--muted);line-height:1.65}.dr2 .method{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px}.dr2 .method div{padding:18px;background:#fff;border:1px solid var(--line);border-radius:17px}.dr2 .method b{display:grid;place-items:center;width:34px;height:34px;border-radius:10px;background:#e4efe6;color:var(--green);margin-bottom:9px}.dr2 .method h3{margin:0 0 6px}.dr2 .method p{margin:0;color:var(--muted);line-height:1.55}.dr2 .templates{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.dr2-template{background:#fff;border:1px solid var(--line);border-radius:22px;padding:22px;box-shadow:0 10px 28px rgba(16,48,28,.05)}.dr2-template header{border-bottom:1px solid var(--line);padding-bottom:14px;margin-bottom:14px}.dr2-template h3{margin:10px 0 5px;font-size:1.45rem}.dr2-template header p{margin:0;color:var(--muted);line-height:1.55}.dr2-id,.dr2-num{display:inline-block;padding:5px 9px;border-radius:999px;font-size:.7rem;font-weight:900}.dr2-id{background:var(--deep);color:#fff}.dr2-num{margin-left:6px;background:#f3edd7;color:#655310}.dr2-fields{display:grid;gap:11px}.dr2-field{padding:12px;border:1px solid #e0e7e0;border-radius:13px}.dr2-field strong{display:block}.dr2-field small{display:block;color:var(--muted);line-height:1.45;margin-top:3px}.dr2-write{height:34px;border-bottom:1px solid #9eaea1;margin-top:7px}.dr2-check{margin-top:14px;padding:14px;background:#eef4ee;border-radius:14px}.dr2-check p{margin:5px 0;color:var(--muted);line-height:1.5}.dr2-choice{font-size:.82rem;font-weight:800}.dr2 .support{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:13px}.dr2 .support a{display:block;padding:17px;background:#fff;border:1px solid var(--line);border-radius:15px;color:var(--green)!important;text-decoration:none!important;font-weight:900}.dr2 .boundary{padding:26px;border-radius:22px;background:#0d2a19;color:#fff}.dr2 .boundary h2{margin:0 0 8px}.dr2 .boundary p{margin:0;color:#d2e0d6;line-height:1.65;max-width:900px}@media(max-width:980px){.dr2 .templates{grid-template-columns:1fr}.dr2 .method,.dr2 .support{grid-template-columns:repeat(2,minmax(0,1fr))}.dr2 .heading{align-items:flex-start;flex-direction:column}}@media(max-width:650px){.dr2 .wrap{width:min(100% - 26px,1180px)}.dr2 .hero{padding:50px 0 43px}.dr2 .section{padding:46px 0}.dr2 .method,.dr2 .support{grid-template-columns:1fr}.dr2 .actions .btn{width:100%;justify-content:center}}@media print{body *{visibility:hidden!important}.dr2,.dr2 *{visibility:visible!important}.dr2{position:absolute;left:0;top:0;width:100%;background:#fff}.dr2 .hero,.dr2 .method,.dr2 .support,.dr2 .boundary,.dr2 .heading>p{display:none!important}.dr2 .section{padding:0}.dr2 .wrap{width:100%;max-width:none}.dr2 .templates{display:block}.dr2-template{box-shadow:none;border:1px solid #999;border-radius:0;margin:0;padding:.35in;break-after:page;page-break-after:always}.dr2-template:last-child{break-after:auto;page-break-after:auto}.dr2-write{height:.42in}.dr2-template header p,.dr2-field small,.dr2-check p{color:#222!important}}</style>`;const method=[['Observe','Describe the change before naming a cause.'],['Measure','Preserve units, location, time and method.'],['Compare','Keep competing explanations until evidence separates them.'],['Verify','Repeat the same observation after the smallest justified change.']];return `${css}<main class="dr2" data-dtf-diagnostic-records-v2="true"><section class="hero"><div class="wrap"><p class="kicker">Teaching Healthy Cultivation · Evidence Records</p><h1>${esc(data.title)}</h1><p class="lede">${esc(data.subtitle)} These worksheets turn the Diagnostic Case Lab method into a repeatable evidence trail you can use at the plant, in a grow log, or on paper.</p><div class="stats"><span class="stat">10 worksheets</span><span class="stat">70 evidence fields</span><span class="stat">10 verification checks</span><span class="stat">Print-friendly</span></div><div class="actions"><a class="btn primary" href="#worksheets">Open worksheets</a><a class="btn secondary" href="/learn/diagnostic-cases/">Practice Case Lab</a><a class="btn secondary" href="/learn/diagnostics/">Diagnostic Index</a></div></div></section><section class="section"><div class="wrap"><div class="heading"><div><p class="kicker">Record method</p><h2>Make your diagnosis reproducible.</h2></div><p>A good record allows another person—or future you—to see what was observed, how it was measured, what alternatives were considered, what changed, and whether the result supported the original interpretation.</p></div><div class="method">${method.map((m,i)=>`<div><b>${i+1}</b><h3>${m[0]}</h3><p>${m[1]}</p></div>`).join('')}</div></div></section><section class="section soft" id="worksheets"><div class="wrap"><div class="heading"><div><p class="kicker">Printable evidence kit</p><h2>Ten records for one complete diagnostic trail.</h2></div><p>Use only the sheets relevant to the case. Printing this page produces one worksheet per page while hiding the navigation sections.</p></div><div class="templates">${data.templates.map(renderTemplate).join('')}</div></div></section><section class="section"><div class="wrap"><div class="boundary"><p class="kicker">Evidence boundary</p><h2>Record facts separately from interpretations.</h2><p>${esc(data.evidenceBoundary)}</p></div></div></section><section class="section soft"><div class="wrap"><div class="heading"><div><p class="kicker">Use the records with the science</p><h2>Jump from the worksheet to the right reference.</h2></div><p>These links connect the records to the live diagnostic, measurement and plant-health curriculum.</p></div><div class="support">${data.supportRoutes.map(x=>`<a href="${esc(x.route)}">${esc(x.label)} →</a>`).join('')}</div></div></section></main>`}
const learnRows=await rowsBySlug('learn');if(learnRows.length!==1)throw new Error(`Expected exactly one Learn page; found ${learnRows.length}.`);const learn=learnRows[0];
const rows=await rowsBySlug('records');const children=rows.filter(p=>Number(p.parent)===Number(learn.id));if(children.length!==1)throw new Error(`Expected exactly one /learn/records/ child page; found ${children.length}.`);const page=children[0];const oldRaw=rendered(page.content);if(!oldRaw.includes('DTF-PUBLIC-LEARNING-PAGE')&&!oldRaw.includes('data-dtf-diagnostic-records-v2="true"'))throw new Error('Records page ownership marker missing; refusing replacement.');
const stamp=new Date().toISOString().replace(/[-:.]/g,'');const backupDir=join(backupRoot,`diagnostic-records-v2-${stamp}`);await mkdir(backupDir,{recursive:true});await writeFile(join(backupDir,'records-before.html'),oldRaw);const newRaw=renderPage();await writeFile(join(backupDir,'records-planned.html'),newRaw);
async function update(content){return request(`/wp-json/wp/v2/pages/${page.id}`,{method:'POST',body:JSON.stringify({title:'Grow Records & Diagnostic Worksheets',slug:'records',parent:learn.id,content,status:'publish'})})}
try{await update(newRaw);const checkRows=await rowsBySlug('records');const check=checkRows.filter(p=>Number(p.parent)===Number(learn.id));if(check.length!==1)throw new Error('Post-write records lookup became ambiguous.');const html=rendered(check[0].content);if(!html.includes('data-dtf-diagnostic-records-v2="true"'))throw new Error('Records V2 marker missing after write.');if((html.match(/data-dr2-template=/g)||[]).length!==10)throw new Error('REST verification did not find exactly 10 templates.');if((html.match(/class="dr2-field"/g)||[]).length!==70)throw new Error('REST verification did not find exactly 70 fields.');if((html.match(/class="dr2-check"/g)||[]).length!==10)throw new Error('REST verification did not find exactly 10 verification checks.');const report={result:'success',route:'/learn/records/',pageId:page.id,...totals,backupDir,publishedAt:new Date().toISOString()};await writeFile(join(backupDir,'report.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2))}catch(error){let rollback='';try{await update(oldRaw)}catch(e){rollback=` Rollback failed: ${e.message}`}await writeFile(join(backupDir,'failure.txt'),`${error.stack||error.message}${rollback}\n`);throw new Error(`${error.message}${rollback}`)}
